name: Feature Branch Check

on:
  push:
    branches:
      - 'feature/**'

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run branch check
        run: echo "Feature branch pipeline is running successfully!"